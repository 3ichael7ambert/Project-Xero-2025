function fix_position() {
	//this is to fix the players position to the planet's surface after landing
	xLength0 = (xLength1+xLength2)/2
	yLength0 = (yLength1+yLength2)/2
	x = xLength0+lengthdir_x(0,gdir-180)
	y = yLength0+lengthdir_y(0,gdir-180)



}
